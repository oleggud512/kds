import 'package:flutter/material.dart';
import 'package:intl/intl.dart';
import 'package:mobile_client/src/core/common/constants/sizes.dart';

const defaultListPadding = EdgeInsets.symmetric(horizontal: p8, vertical: p4);
const defaultListSeparator = h4gap;
const dividingListSeparator = Divider(height: p4);
const defaultContentPadding = EdgeInsets.all(p16);

final defaultDateFormat = DateFormat("dd/MM/yy HH:mm");